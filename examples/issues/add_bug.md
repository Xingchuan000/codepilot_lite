# Add function returns wrong result

The `add(a, b)` function currently returns subtraction instead of addition.

## Expected

`add(1, 2)` should return `3`.

## Reproduction

Run:

```bash
python -m pytest -q
```
